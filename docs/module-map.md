# Runtime Module Map

This file is the implementation index for the current production slice. The accepted
design documents and ADRs in the VibeMate design repository remain the
architecture authority. This map records code ownership and current evidence;
it does not redefine product scope.

| Module | Responsibility / non-responsibility | Typed input, output, and authority | Lifecycle | Evidence and current slice |
|---|---|---|---|---|
| `internal/productruntime` | Owns the only production business-component composition sequence, incarnation, internal initialization state, rollback, and shutdown ordering. It composes credential control, both capture authorities, Exchange, the revision-aware local certificate authority, and the handler-only proxy, but does not bind listeners, publish product readiness/discovery, install system trust, or implement UI. | Consumes `Options`, host contract, runtime paths, one `RuntimeCoordinator`, one SecretStore, approval and Exchange policies, clock, ID source, and security random source. Produces `Runtime`, `RuntimeStatus`, Access writer/resolver and projection health, credential controller, Exchange executor, CaptureRun and ManualCapture controllers, ConnectionEvent reader, immutable Root identity and defensive-copy public certificate delivery material, proxy handler, Activity reader, tool-approval controller, and typed offline enter/resume methods. Provider and original-origin clients remain private implementation dependencies. | Created by `Start`; only fully constructed components enter bounded LIFO rollback/shutdown. Startup restores both capture authorities before proxy construction. Shutdown closes proxy, ManualCapture, CaptureRun, offline, and Exchange admission; active CaptureRuns are revoked while ManualCaptures retain their declared durable lifetime; transports and hijacked operations are closed and drained before lower durable owners. Cleanup failure leaves `stop_failed`; the tree is not automatically retried. | Lifecycle, Root identity continuity across normal reopen, complete aggregate recovery/recompilation, exact resume-target composition, real Access-to-Exchange composition for both typed client paths, credential metadata/write composition, both disjoint capture credentials through one proxy admission, provider-body cancellation, host contracts, and shutdown-order tests. Product readiness remains Host-owned. |
| `internal/access` | Owns the typed Access aggregate, separate client/provider network identities, explicit capability catalog, pure compiler, deterministic `PlanHash`, aggregate-local CAS orchestration, immutable active-plan projection, exact `ClientOrigin` lookup, per-Access projection trust, downstream-protocol intersection, and the sole leaf-issuance authorization cut. It does not own SQLite, certificate keys, secret values, protocol wire I/O, provider transport, or control handlers. | Consumes a typed repository, compiler catalog, versioned client-operation and upstream-presentation definitions, one typed Root revision, a leaf-cache invalidator, and the plan projection. Produces a writer, sole `SnapshotResolver`, typed ingress lookup/catalog boundaries, the protocols executable by every currently selectable profile, frozen provider probe identities, one product-level presentation with protocol-keyed variants, one-use projection-owned leaf admissions, and projection health keyed by `AccessID`. A typed leaf intent is connection evidence rather than authority; only the current projection can turn exact Root/Access/endpoint/origin/SAN/algorithm evidence into an admission. SQLite aggregates are durable authority; the atomic plan projection is read-only process state. | Restores and recompiles every valid committed aggregate during startup; no rows and durable disabled rows are valid inactive states. Validation and compilation happen before mutation admission. Writer ownership spans CAS through active-plan publication or withdrawal. Publication is the endpoint-revocation cut; replacement or disable synchronously invalidates derived leaves, and revision tombstones prevent stale reactivation. An indeterminate commit or failed post-commit publication poisons only the affected Access ID. Shutdown rejects new writes, cancels pre-commit work, and drains post-commit publication. | Ownership/reference/operation/capability rejection, deterministic hashing, presentation-mode/product/protocol-variant validation, route-aware H1/H2 intersection, provider-dialect independence, input/getter alias isolation, stale/concurrent CAS, old/new handles, ClientOrigin conflicts, one-use and forged leaf admission rejection, stale Root/endpoint, bare-IP DNS reclassification, and typed IP admission rejection, publication/withdrawal cuts, disabled close/reopen recovery, multi-Access poison isolation, injected commit ambiguity/publication failure, and race tests. Current compiler accepts one profile/account/route, Direct egress, fixed model, either Anthropic Messages or exact OpenAI Responses as the client operation against OpenAI Chat, the default H1/H2 follow-client presentation or explicit H1 Claude Code presentation, and an empty pass-through plugin plan. Endpoint deletion has no writer/control operation in this slice; named Codex H2 presentation is deliberately absent until its captured wire shape is executable. |
| `internal/workspaceidentity`, `internal/workspaceroute` | Own the local installation identity, opaque exact-directory workspace identity, and the durable Access-scoped default-profile binding. They do not authenticate an OS user, infer a Git root, choose an Access before AgentEndpoint resolution, widen an Access RouteSet, or persist absolute workspace paths in route rows. | Workspace identity consumes one private random machine identity/namespace key plus a canonical local cwd and produces a typed `(MachineID, WorkspaceID)` scope. Route resolution consumes that scope and one already-resolved immutable Access snapshot, lazily creates or reads `(AccessID, MachineID, WorkspaceID)`, and returns a revision-pinned profile lease. | ProductRuntime owns both managers. Identity creation is atomic and installation-persistent; route records are SQLite authority. Each Exchange releases its pin at completion. Route CAS affects later resolutions only; shutdown follows the runtime/store owners. | Identity permission, atomic-create, canonical-path/HMAC isolation, corruption/loss, SQLite reopen, default creation, stale/concurrent CAS, unavailable-profile repair choices, old-request pinning, and ProductRuntime route-switch tests. This is local Desktop evidence only; remote machine enrollment is absent. |
| `internal/accessapply` | Owns the apply-only translation from one complete control-plane DTO to the authoritative Access aggregate. It does not persist, publish, expose a draft model, or define an M0-only singleton API. | Consumes a typed path `AccessID`, expected aggregate revision, and complete value DTO. Produces one `access.WriteCommand`; every owned child receives the same next revision. | Pure construction with no background work or external resources. | Identifier, ownership, origin, provider target, model, and alias-isolation tests. State: the authenticated Desktop apply route consumes this boundary; draft editing remains absent. |
| `internal/protocolcore` | Owns immutable protocol-neutral messages, content blocks, separate item/call identities, tool calls/results, known/unknown usage details, model identities, stop semantics, translation notices, and language-independent failures. It does not parse HTTP, perform I/O, choose an Access, or approve tools. | Constructor-validated values cross codec boundaries; owned byte and collection values are defensively copied, JSON objects reject duplicate names, and failed/cancelled outcomes cannot masquerade as successful response stop reasons. | Pure values with no background work or external resources. | Deep-copy, known-zero versus unknown usage, item/call separation, duplicate correlation rejection, response/tool consistency, provider-extension isolation, fuzz, and race tests. |
| `internal/protocolpath` | Owns the typed composition boundary between one client protocol edge and one provider protocol edge around neutral IR. It does not resolve plans, perform transport I/O, retrieve credentials, or provide a global codec registry. | Explicit `ClientCodec` and `BackendCodec` interfaces decode and encode immutable protocol-core values and return explicit translation reports. A client stream encoder is injected into the existing backend stream decoder. Each typed path binds exact client-operation IDs and validates the frozen Access codec ID, revision, operations, and both dialects; an explicitly assembled immutable selector chooses the one matching path. Provider requests own cloned method/path/header/body state. | Pure composition with no background work. | Constructor/duplicate rejection, operation/dialect/revision mismatch, request alias isolation, response-report propagation, and bilateral codec tests. The streaming bridge remains an explicit pair-specific seam. |
| `internal/ssewire` | Owns bounded incremental Server-Sent Event framing. It does not interpret protocol JSON, infer success from EOF, reconnect sockets, or perform network I/O. | A caller-owned decoder accepts arbitrary byte fragments and emits only complete blank-line-terminated events under explicit byte/event limits; the encoder emits canonical frames. | Caller-owned state only; no goroutines or external resources. | LF/CRLF, multiline fields, byte fragmentation, limits, partial EOF, round-trip, fuzz, and race tests. |
| `internal/anthropicchat` | Implements the bounded Anthropic Messages client edge and the reusable OpenAI Chat backend edge, including the sole Chat response/SSE decoder and complete-tool barrier. It does not select plans, retrieve credentials, send HTTP, or localize errors. | The Anthropic client edge converts wire values to and from protocol-core IR. The Chat backend builds frozen requests, uses an explicit per-request tool catalog, and drives an injected typed client stream encoder only after validated semantic transitions. Official SDKs remain test-only oracles. | Pure request/response and incremental stream state owned by the caller. | SDK-oracle fixtures, per-byte streaming, model separation, complete/incomplete/parallel and custom/namespaced tools, unknown provider-tool rejection, no-leak decisions, unsupported-shape failures, fuzz, and race tests. No network execution is claimed. |
| `internal/openairesponses` | Owns the typed OpenAI Responses client wire edge. It does not send provider traffic, choose Access, retrieve credentials, decode provider Chat traffic, or import the official SDK in production. | Bounded strict JSON is decoded into immutable protocol-core messages with distinct item/call identities, top-level instructions, function/custom/namespace tools, reasoning intent, and explicit translation notices. A message item ID that Chat cannot preserve and a supported hosted `web_search` declaration that Chat cannot execute are omitted explicitly rather than forwarded as false semantics; unknown hosted-tool fields fail closed. Complete JSON and incremental named SSE events are encoded with deterministic client-safe response/item/call identities, strict ordering, bounded state, and explicit usage approximations where the Responses wire requires an integer that the Chat backend did not report. Stored/background execution is rejected explicitly. | Pure caller-owned codec and stream-encoder state; no goroutines or external resources. | Fixed and current Codex request shapes, complete response, text/function/custom SSE, function output, required terminal usage fields, failure/cancellation oracle fixtures; malformed ordering, duplicate terminal, unknown extension, incomplete correlation, alias, fuzz, and race tests. State: the exact HTTP create operation is selected by production Exchange/ingress, and one clean packaged fixed-client report proves the supported HTTP vertical. Successful Responses WebSocket semantics remain unimplemented. |
| `internal/responseschat` | Explicitly composes the Responses client edge with the existing Chat backend edge. It does not own a registry, transport, Access selection, credentials, approval, or ingress. | A typed constructor produces one versioned, operation-bound `protocolpath.Path`; Chat custom/namespaced tools are reversibly represented through a per-request catalog, while item identity and unsupported controls produce stable translation notices. | Pure composition with caller-owned stream state and no background work. | Fixed request-to-Chat body, history correlation, deterministic complete response, incremental text, approval-fenced custom tool, unknown provider-tool, and production selector tests. State: its identity, revision, dialects, and exact create operation compile into the sole Access plan and are selected explicitly by production Exchange. |
| `internal/exchange` | Owns one frozen-plan execution pipeline, action admission, ingress-binding and client-operation revalidation, provider attempt accounting, retry safety, downstream publication, stream idle budgets, translation evidence, and the tool-decision barrier. It does not own a listener, resolve SQLite directly, mutate Access, retrieve a secret directly, or expose a provider bypass. | Consumes `ClientRequest` with independently generated identity plus one immutable route-neutral CaptureAdmission and a separate connection reference, immutable ingress and exact operation evidence, the sole `SnapshotResolver`, an attempt-identity source, one explicitly assembled typed protocol-path selector, mandatory action admission, gated provider, tool-decision gate, attempt observer, and bounded policies. Optional run/manual and workspace evidence is derived from the admission rather than supplied again. It resolves exactly once, passes only frozen plan values downstream, and returns merged immutable translation notices without semantic payloads. | Each Exchange has an owner-derived context and an action lease. `BeginShutdown` rejects and cancels operations; transport closure can release blocked bodies before bounded `Drain`. | Messages and Responses complete/streaming output, route-neutral ingress correlation, report propagation, one-resolve proof, operation and stale-endpoint rejection, provider-only plan revision compatibility, retry ledger after Responses-visible output, semantic idle, malformed streams, keepalive cancellation, both client tool barriers, planned hold/resume, shutdown, concurrent multi-Access isolation, and race tests. State: executable for exact frozen Messages and Responses HTTP operations through the same gated provider boundary. |
| `internal/capturecredential` | Owns the closed wire shape and disjoint type tags of proxy capabilities. It does not authenticate, persist, select a route, or carry attribution/business metadata. | Consumes exactly 256 bits of entropy and one closed kind; produces canonical `run_…` or `manual_…` credentials with redacted formatting. | Pure immutable values. | Round-trip, canonical-form, unknown-tag, cross-kind, and redaction tests. State: both production capture authorities issue this shape and the shared admission dispatcher consumes it. |
| `internal/captureadmission` | Owns the immutable result of authenticating one proxy credential. It records how traffic entered VibeMate and cannot select Access, Profile, route, account, model, plugin, or provider credential. | Consumes a redacted typed proxy credential through one composite `Authorizer`. The closed credential tag selects exactly one durable CaptureRun or ManualCapture authority without fallback; successful evidence becomes a managed or configured-manual admission. | Values are immutable and request/connection-scoped; the underlying capture authorities own credential lifetime and shutdown. | Disjoint credential dispatch/no-fallback, rejection/redaction, configured/verified managed mapping, manual non-escalation, adapter clone isolation, workspace identity, malformed evidence, ProductRuntime proxy integration, structural fixtures, and race tests. State: both durable authorities are wired to the one production proxy. |
| `internal/manualcapture` | Owns durable, revocable proxy capabilities for independently launched CLI and desktop applications. It does not parse HTTP, select Access/Profile/routes, infer an application or process, retain plaintext credentials, or define product copy. | Consumes an owner-scoped typed create/rotate/revoke/read command and a SQLite repository. Produces one-time redacted grants, stable `manual-capture/<capture-id>` ingress identity, a Core-only credential epoch, configured source label, lifecycle state, and authenticated evidence. Ownership is either this local installation or one exact future ProxyClientBinding. | ProductRuntime restores it before proxy construction. Authorization alone records real traffic observation. Shutdown rejects and drains work but leaves active records to their declared lifetime or explicit revocation. | Digest-only storage, create/authorize/rotate/revoke, stale and concurrent CAS, owner isolation, exact expiry, close/reopen recovery, one-time secret/log redaction, ProductRuntime/shared-proxy composition, lifecycle cancellation, and race tests. State: data-plane authority and authenticated local control adapter are wired; Desktop UX and remote ownership are absent. |
| `internal/proxyclient` | Owns durable remote-client binding, one-time enrollment, machine registration, and enrolled control identity. It does not bind a listener, deliver a Root, issue a proxy grant, store a raw credential, trust network location, or select an Access, Profile, route, account, model, plugin, or provider credential. | Consumes an immutable binding policy, internal aggregate generation, public MachineID/display metadata, two disjoint typed credential namespaces, and a SQLite repository. Produces redacted immutable views, one-time enrollment/control delivery, and the existing immutable `controlprincipal.Principal`; allowed grant kinds are derived only from binding authority. | Its lifecycle gate rejects new work, cancels active work, and drains within the caller deadline. The repository is exposed through RuntimeStore for the next Server composition slice, but ProductRuntime does not yet construct the Manager. Binding revocation closes pending enrollment, machine, and principal state in one transaction. | Canonical immutable policy, namespace separation/redaction, digest-only persistence, expiry, wrong-credential zero side effects, concurrent one-winner completion, close/reopen authentication, revocation cascade, ambiguous-commit reconciliation, shutdown cancellation, and race tests. State: durable Core authority only; no remote endpoint or traffic path is wired. |
| `internal/capturerun` | Owns short-lived child attribution, separate proxy/control bearer capabilities, stable local machine/workspace references, the display-only launcher user label, and whether traffic was actually observed through a run. It never treats the user label as identity, persists raw capabilities, child arguments, credentials, or client semantic payloads, and does not launch processes or expose HTTP. | Consumes typed create/attach/heartbeat/finish commands, the consulted client-catalog revision, optional verified adapter evidence, optional local workspace scope, and a SQLite repository. Produces one-time `LaunchGrant`, redacted views, and frozen proxy authorization evidence. SQLite stores domain-separated capability hashes plus non-secret run/catalog/compound-release/workspace evidence. | Observation is marked only from a real authenticated proxy connection, is monotonic and idempotent, and cannot be set on a run that is no longer active. Startup expires stale records. Admission and active operations are bounded; shutdown rejects new work, drains current calls, and revokes every active run. | Capability separation/rejection, generic and verified adapter binding, user-label identity non-interference, expiry/recovery, workspace schema upgrade, attach conflicts, heartbeat, shutdown revocation, close/reopen, alias isolation, and race tests. State: composed into ProductRuntime and exposed only through launcher/run-scoped DesktopHost routes. |
| `internal/connectionevent` | Owns the durable body-free connection phase timeline. It does not store URL paths, headers, bodies, raw capabilities, TLS bytes, or semantic Exchange content. | Consumes typed attempted/decided/connected/terminal evidence and a SQLite repository. Produces immutable paginated records and bounded per-connection timelines. | Startup appends failed terminal records for interrupted connections. New operations are rejected during shutdown and active journal writes drain within the caller context. | Phase-transition, redaction, pagination, interrupted recovery, alias isolation, shutdown, repository reopen, and race tests. State: loopback proxy journal is wired. |
| `internal/operationcatalog` | Owns the explicit, versioned built-in client API operation definitions. It does not select an Access, instantiate codecs, perform dispatch, or provide a global implementation registry. | A typed constructor produces immutable definitions covering dialect, method, exact/prefix path, HTTP/WebSocket transport, semantic category, body/replay limits, codec feature, query policy, `OperationPayloadClass`, and egress bearing. Payload class is orthogonal to operation kind: `count_tokens` is an auxiliary operation whose body is the complete messages, system text, and tool schema, so it is declared `client_semantic`. Access compilation and path classification consume the same definitions. | Pure immutable values; no resources or goroutines. | Exact Responses and Messages HTTP operations, explicit unsupported Responses WebSocket upgrade, control-plane opaque classification, upload/background-adjacent/Realtime/foreign-semantic rejection, ID uniqueness, and alias tests. |
| `internal/pathcapability` | Compiles the operation catalog into canonical method/path/query dispatch. It does not define model API paths, parse request bodies, choose Access plans, perform I/O, or register codec implementations globally. | Consumes immutable `access.ClientOperationDefinition` values and classifies a frozen client dialect and canonical request target into semantic, auxiliary, opaque, or unsupported capability plus operation identity, revision, transport, replay/body limits, frozen `OperationPayloadClass`, and egress policy. An uncatalogued path reports the unknown payload class rather than a permissive default. Exact definitions sharing a path remain distinct by canonical method. | Pure immutable catalog; no resources or goroutines. | Messages semantic/auxiliary, exact Responses HTTP semantic and unsupported WebSocket classifications, opaque control/unknown fallback, explicit management/upload/media/batch/Realtime/foreign-semantic rejection, malformed target, method/query mismatch, catalogued and uncatalogued payload class, alias, and race tests. State: consumed by the production proxy and Exchange operation-evidence boundary. |
| `internal/localca` | Owns one installation-persistent ECDSA P-256 Root key/certificate, its immutable public DER-derived identity and persistent revision, crash-safe manifest migration, and revision-authorized exact-DNS leaf issuance. Root private-key types and bytes remain confined to this package. It does not install trust, alter OS settings, decide Access authorization, issue production IP leaves, rotate the Root, or own a listener. | Consumes an explicit private directory, owner context, clock, security random source, bounded cache capacity, generation deadline, and one-use projection-owned leaf admissions. Produces immutable public `RootIdentity`, defensive-copy `RootCertificate` delivery material, and independently owned TLS certificates. Certificate DER SHA-256 is the only machine Root identity; fingerprint is derived and the delivery path is not identity. | Open validates or atomically creates private Root artifacts. A v1 manifest is migrated in place to v2 with same-directory temporary write, file sync, atomic rename, and directory sync while preserving the exact key/certificate. The process-local bounded LRU and same-key singleflight are derived state; different keys may generate concurrently. Authority-owned generation outlives an individual waiter only within its deadline. Invalidation cannot resurrect cache. Bounded retryable shutdown rejects admission, cancels generation, drains work, and clears derived state. | Exact key/certificate/revision continuity, DER/manifest binding, permission/symlink/unknown/trailing/partial-state rejection, injected atomic migration failure recovery, exact DNS SAN, unforgeable/one-use admission, bounded eviction, same-key coalescing, different-key parallelism, independent waiter cancellation, panic/random/signing/timeout retry, revocation during generation, returned-value isolation, owner cancellation, bounded shutdown, and race tests. Normal reopen and injected filesystem boundaries do not prove physical crash or power-loss durability. System trust installation remains deferred. |
| `internal/systemtrust` | Owns the platform-neutral exact-presence/trust-decision contract, immutable current-Root install/remove plans, and fixture-backed macOS operation orchestration. It does not provide a live executor, inspect or mutate an operating-system trust store in production, persist a second Root state, read a private key, or expose Runtime, Control API, Tauri, or UI capability. | Consumes only a sealed current-public-Root source backed by `localca.Authority`, the fixed typed admin-trust/System.keychain/Server-TLS target, and one injected command executor. Produces exact revision/digest-bound observations, ordered typed plans, and redacted typed results. Certificate DER SHA-256 remains the only machine identity; certificate paths, subject, serial, output, and display fingerprint are not authority. | Planning reads current public Root and bounded fixture evidence. Execution uses fail-fast process-local ownership, revalidates Root and complete precondition, reinspects after every mutation step with an independent hard deadline, and stops later destructive steps after any non-success. Shutdown closes admission, cancels the active command, waits through reconciliation, and is bounded, idempotent, and retryable for contract-compliant executors. | Truth-table/idempotency, unknown/contradictory evidence, same-subject foreign DER, stale revision/digest/precondition, fixed no-shell command shapes, private public-certificate artifact cleanup, failed/indeterminate/cancelled/timeout reconciliation, per-step stopping, fail-fast concurrency, retryable shutdown, alias isolation, structural non-composition, and race tests. The macOS trust grammar and `-p ssl` mapping are versioned fixtures only. No production path invokes `security`; live authorization, trust decision, removal, and client TLS consumption remain unverified. |
| `internal/loopbackproxy` | Owns authenticated CONNECT handling, ClientHello observation, canonical AgentEndpoint matching, route-aware downstream ALPN intersection, projection-owned leaf admission, MITM request dispatch, persistent-connection endpoint revalidation, semantic Exchange entry, separate agent-probe and opaque original-origin dispatch arms, local dialect-shaped rejection of payload-bearing operations without a typed handling plan, explicit unsupported-upgrade rejection, and ConnectionEvent updates. It evaluates connection policy before any dial, forwards an unmatched CONNECT blind, and forwards an authenticated cleartext proxy request to its own origin. It does not bind a listener, publish discovery, install a Root, retrieve provider secrets, import the CaptureRun aggregate, or decide routes. | Consumes a route-neutral `captureadmission.Authorizer`, the combined active `IngressAuthority` resolver/admitter/protocol resolver, immutable path catalog, Exchange executor, gated original client, revision-aware local CA, ConnectionEvent journal, and Exchange ID source. Proxy authentication resolves before policy, dial, DNS, certificate issuance, AgentEndpoint lookup, or body read. After CONNECT authorization, the real TLS ClientHello must repeat the exact canonical DNS identity; the current Access projection must admit at least one offered application protocol executable by every selectable profile before the proxy asks it for a one-use Root/Access/endpoint/origin/SAN/algorithm admission. Admission is decided from the frozen payload class before any body is read, so a request without a typed handling plan is drained within its catalogued limit and rejected in the client dialect's own error envelope with the stable reason in `X-Vibermate-Reason`; its body never enters a retained buffer, log, error, record, or row. Semantic requests carry only owned body, capture admission, ingress, ClientHello, and typed operation evidence across the Exchange boundary; client auth and hop-by-hop headers do not. | Runtime owner cancellation closes admission and hijacked connections. `BeginShutdown` rejects new CONNECT work; `Drain` waits for bounded active operations. Leaf generation is owned and drained by the later local-CA cleanup stage. | Auth-before-cert/data-plane, exact origin/CONNECT/SNI/SAN, route-aware H1/H2 negotiation and no-common-protocol refusal, canonically equivalent authority forms matching without widening, real authorized CONNECT/MITM, revocation between CONNECT and ClientHello, exact Messages/Responses dispatch, adapter-feature-gated bounded Responses WebSocket 426, generic-client 422 rejection, stale endpoint rejection on reused connections for both dialects, audit persistence, hijacked-connection shutdown, route-neutral Exchange correlation, structural rejection of direct CaptureRun imports, and race tests. State: DesktopHost owns its literal loopback listener and publishes it only after route completion. The clean deterministic packaged report bound to `c16b2f9556fd4123adba9d5a6f72bb7fbb855905` exercises the current managed-run path through fixed Claude Code 2.1.220. It does not prove arbitrary-client compatibility. |
| `internal/egressaudit` | Owns the immutable per-egress attempt record and its bounded query surface. It does not own a connection timeline, select a route, dial, or hold any secret, header, body, or tunnelled byte. | Consumes typed purpose, payload class, parent reference, target origin, and egress decision. Construction proves purpose-to-authority agreement, the one parent shape each purpose admits, that an original-origin or agent-probe record cannot claim client payload, that runtime egress carries no client connection, and that an attempt identity does not encode its parent. `Finish` returns a new terminal value rather than mutating. | Pure immutable values; the SQLite repository owns durability. | Purpose/authority agreement, exhaustive parent combinations, payload-class refusal, identity independence, caller identity, double-terminal refusal, persistence and reopen, unique identity, marked pool reuse, and connection/parent filters. State: both the provider and original-origin transports emit one record per real outbound through the production runtime. Blind tunnels record their own; runtime-originated egress has no writer yet. |
| `internal/connectionpolicy` | Owns the stored rule set, the decision for one proxied connection, and the set in force while the runtime runs. Rules are replaced whole, so a set that would not construct is refused before anything is written and the set in force is only ever one that was successfully stored. It owns no transport, performs no I/O, and consults nothing outside the frozen set, so the same set and the same connection always produce the same answer and name the same rule. | Consumes a host and port. Produces a decision plus the rule identifier that produced it. Match kinds are closed to any, exact host, and exact host and port: a rule cannot carry a pattern language, because a wildcard is how an allow list quietly becomes an allow everything. | Pure immutable values. | Declared precedence with an identifier tie-break, determinism, declared default, refusal of a wildcard allow default, `ask` as a first-class decision, refusal of a stored or edited set that would not construct, and port narrowing. State: evaluated before every dial by the production proxy, stored and editable through the control API, reloaded when an answer is remembered; the shipped set allows nothing in advance and asks about an undecided host. |
| `internal/blindtunnel` | Owns forwarding a connection whose authority is not an enabled AgentEndpoint, and the gated dial that opens its upstream side. It does not terminate TLS, parse a request, decide whether a connection is allowed, or retain a tunnelled byte. | Consumes a frozen authority, an action lease, and the egress coordinator. Produces an upstream connection plus the lease the caller releases, and byte counts in each direction. The resume probe is raw TCP because reachability is all a tunnel can establish. | Copy returns when both directions finish; owner cancellation releases both through deadlines. The caller owns closing and appends the immutable EgressAttempt before invoking the dialer, then completes both the attempt and client-side ConnectionEvent from the same terminal classification. | Bidirectional copy and counts, audit-before-dial refusal, consistent completed/failed/canceled terminals, owner cancellation, peer close, raw-TCP probe admission limited to blind tunnels, and race tests. State: wired into the production proxy through the gated dialer. |
| `internal/activity` | Owns the durable redacted operational timeline. It does not store prompts, completions, credentials, headers, raw tool arguments, raw ClientHello, or provider error bodies. | Consumes validated stable event kinds, identifiers, language-independent reason codes, and optional redacted product-presentation and lower-transport evidence. Produces immutable paginated records. | Operations use the shared SQLite admission gate through the repository; the manager rejects new work and drains active records during shutdown. | Pagination, close/reopen persistence, input/output alias isolation, presentation/transport-evidence validation, shutdown, and race tests. Exchange attempts are wired through ProductRuntime. |
| `internal/toolapproval` | Owns durable fail-closed approval. A record declares its kind and derives risk, copy keys, and choices from it, so a second kind does not fork the record; an Access plan binding is required only for a kind decided after Access resolution. It does not expose raw arguments, execute tools, or grant persistent policy. | Consumes a kind, a stable aggregate key that merges identical pending questions with request and waiter counts, redacted subject references and safe display labels, and for tool intent a frozen Access revision and PlanHash. Produces pending views with stable i18n keys and an allow-once or deny decision for the complete group. SQLite is the decision authority. | Startup cancels orphaned pending approvals. Each caller waits with a deadline; decisions are CAS- and idempotency-keyed. Shutdown cancels pending rows, releases waiters, rejects admission, and drains active work. | Complete-group blocking/release, denial, idempotent replay, stale decision rejection, timeout/cancellation, recovery, redaction, authenticated control decisions, UI binding, and race tests. Separate clean packaged reports establish the fixed Claude and fixed Codex client paths; they do not generalize to arbitrary releases. |
| `internal/runtimepersistence` | Owns SQLite connection policy, the complete unreleased schema baseline, operation admission/cancellation/drain, runtime metadata, complete Access aggregate rows, transactional CAS, Activity, tool approval, CaptureRun, ManualCapture, ProxyClient binding/enrollment/machine/principal, ConnectionEvent, EgressAttempt, and workspace-route rows, plus schema-state reads. It does not compile or publish active plans, retain raw capabilities, retrieve secrets, send provider traffic, provide backups, or expose UI queries. | Consumes an absolute database path and bounded connection/commit-reconciliation policy. Produces a file-backed store, `SchemaStateReader`, and typed Access, Activity, approval, CaptureRun, ManualCapture, ProxyClient, ConnectionEvent, EgressAttempt, and workspace-route repositories. Schema revision 1 plus a fixed baseline identity are the current unreleased database authority; Access, workspace bindings, capture credentials, and remote-control credentials own separate live business/security generations. | Opened first; the one complete baseline is installed before initialization. Pre-baseline development databases and newer Goose history fail closed rather than entering a compatibility path. CaptureRun, ManualCapture, enrollment, and enrolled-principal rows retain only digest/non-secret evidence. All repository work uses one operation gate. Ambiguous Access and one-time enrollment commits are reread using a bounded store-owned context. Shutdown closes admission, cancels and drains owned operations, then closes SQLite. | File-backed reopen, baseline identity rejection, pragmas, permissions, held-transaction deadline, complete aggregate CAS/rollback, ClientOrigin uniqueness, commit reconciliation, workspace-route and ManualCapture stale/concurrent CAS and reopen, one-time remote enrollment/concurrent consumption/revocation, Activity, approval/CaptureRun/ConnectionEvent/EgressAttempt persistence and recovery, corruption, cancellation, and schema continuity tests. State: persistence and both capture repositories are composed into ProductRuntime; the ProxyClient repository is exposed only for a later Server composition. |
| `internal/offlinehold` | Owns the mandatory external-egress admission seam and planned enter/drain/probe/FIFO-release state machine. Action membership is proved by the typed `ActionLease` the caller holds, never reconstructed from an identity prefix. It does not own sockets, secrets, OS reachability, persistence, reactive cursor resume, or UI. | A logical action and Enter share one mutex cut: actions admitted before Enter may finish, while later actions queue at egress. Every acquire carries that action plus a complete frozen non-secret network identity; provider identities bind Access revision and PlanHash. Queues are count/byte/time bounded. | One runtime incarnation binds the gate. Enter waits for pre-cut actions and active egress before `SafeToDisconnect`; Resume probes exact queued identities and releases only successful targets. Shutdown rejects queued work and drains actions and leases. | Deterministic pre/post-Enter ordering, plan-change target isolation, Safe-to-disconnect fencing, failed-probe zero release, bounded FIFO/concurrency, cancellation, revision conflicts, alias isolation, shutdown, and race tests. Physical network change remains unverified. |
| `internal/secretstore` / `internal/hostsecret` | Define canonical `SecretRef`, destroyable bounded values, redacted metadata, the typed host Factory/Store boundary, and a cross-platform development file driver. They do not put secrets in SQLite, log/list values, return values to UI, or select a backend inside ProductRuntime. | ProductRuntime receives exactly one explicit Store. Ordinary builds select a private file factory with `!vibermate_native_secrets`; its CAS document is plaintext-equivalent at rest. Native release selection is not present in this stage. | Values are caller-owned and explicitly destroyed. The development Store serializes CAS and replaces a private file atomically for the host platform; reopen reconstructs secret revisions. | Reference/value destruction, metadata/CAS conflict, permissions, symlink/path rejection, replacement failure, concurrent access, close/reopen, and race tests. Development storage is not release protection. |
| `internal/accesscredential` | Binds credential metadata and write-only replacement to account bindings in the active Access plan. It does not return `SecretRef` or secret bytes, inspect SQLite, select a backend, or read a secret merely to render control state. | Consumes the sole `SnapshotResolver` and one explicit SecretStore. Produces redacted state/revision views and CAS replacement results scoped by `AccessID`, profile ID, and account binding ID. | Request-scoped; ProductRuntime owns the underlying resolver and SecretStore lifetimes. | Active-plan ownership, cross-profile rejection, metadata-only inspection, unavailable mapping, replacement CAS, response redaction, and race tests. |
| `internal/transportprofile` | Owns frozen observed/named/standard TLS mechanism material, strict connector construction, and redacted lower-transport evidence. It does not choose the product-level presentation, select Access plans, retrieve credentials, or claim raw ClientHello passthrough or exact browser/Agent fingerprint fidelity. | Consumes the selected protocol variant's transport plan plus an observed ClientHello where requested. Produces a strict verified TLS connection and immutable requested/effective/fallback/ALPN/HTTP evidence. MITM always creates a new upstream TLS connection: safe cipher/extension shape may be retained, while SNI/ALPN are authority/transport scoped and random, key share, ticket, PSK, binder, and other connection state are new. | Each connector operation is context-bounded; no global pool, registry, or background owner exists. | ClientHello capture bounds, extension sanitization, SNI/ALPN replacement, TLS-version enforcement, exact negotiated-protocol enforcement, strict certificate rejection, and race tests. Black-box local TLS services observe actual H1 and H2 outbound ClientHellos, verify retained cipher-suite/extension ordering, and verify changed SNI/ALPN/random/key-share state. The product catalog currently supplies no fallback chain. This does not prove exact browser/Agent fingerprint parity or H2 SETTINGS/header-order parity. |
| `internal/providertransport` | Owns the final M0 provider request, product-level upstream presentation selection, AuthDriver application, typed strict-TLS versus literal-loopback-cleartext dispatch, response-idle ownership, typed resume probing, and shutdown. It does not choose a presentation from provider/model/dialect identity, resolve Access plans, translate protocol semantics, expose a raw client, or permit non-loopback cleartext. | Consumes a frozen compiled target/action, the mandatory coordinator, one typed authenticator, SecretStore reader, user-selected presentation profile, downstream application protocol, observed ClientHello, and the current request's bounded User-Agent observation. It resolves the exact same-protocol variant before secret access or dial; a missing variant fails closed without switching protocol, product, or standard transport. Follow-client preserves that validated UA or its absence and never copies arbitrary headers; named products use their evidence-bound constant. Egress lease acquisition precedes secret read and any outbound byte, and one immutable EgressAttempt is appended before the first byte and completed on every terminal. Origin, HTTP authority, network host, and optional SNI remain separate frozen fields. Remote targets use strict system-root TLS and the selected H1 or H2 transport variant. The Direct-only loopback exception disables ambient proxies and verifies the exact TCP peer before authenticated HTTP write; it currently accepts only H1, with H2 rejected during frozen request construction. | Operations are admitted into the client lifecycle, hold action/egress leases through response EOF or close, and are canceled and drained during shutdown. Response EOF completes the attempt; body read failure, timeout, cancellation, and shutdown produce their corresponding failed or canceled terminal rather than a false success. | Default H1/H2 follow-client and explicit named-product selection, protocol-variant and cleartext-H2 refusal before outbound work, bounded UA preservation and AuthDriver mutation refusal, product presentation evidence, lease-before-secret/transport ordering, target/path/Host/SNI/`:authority`, remote/localhost/LAN HTTP rejection, literal-loopback peer validation, no-write on peer mismatch, redirect denial, header stripping, value destruction, strict TLS, real H2 request and incremental response delivery, transport-appropriate no-credential probes, response-body terminal classification, idle timeout, cancellation, shutdown, and race tests. Generic H2 uses the fixed `x/net/http2` serializer and does not claim observed SETTINGS/window/header-order fidelity. ProductRuntime Exchange invokes this typed boundary. |
| `internal/originaltransport` | Owns strict TLS forwarding and probing for approved non-model original-origin operations. It does not select an Agent endpoint, classify paths, carry provider credentials, permit ambient proxy behavior, or accept a request that carries client payload. | Consumes a fully frozen request carrying typed connection and original-request references, an action lease, exact origin/authority/SNI, a mandatory coordinator, and an audit writer. Construction re-proves the payload invariant: only a catalogued `none`/`control` class may carry its own control body, an unclassified request is admitted solely when its empty body establishes payload-freedom, and `client_semantic`/`client_data` are refused outright. It returns a response whose body owns the egress lease. | Client admission, cancellation, body closure, response terminal classification, and bounded shutdown mirror the provider transport. EOF completes the audit; read failure, timeout, cancellation, and shutdown cannot be recorded as success. | Lease ordering, TLS verification, redirect denial, header/body ownership, payload-class admission and refusal, exact probe target, response-body terminal classification, cancellation, shutdown, and race tests. The loopback proxy invokes it only after CaptureRun, endpoint, path, and payload-class authorization. |
| `internal/controlprincipal` | Owns authenticated control-plane identity and one digest-only credential authority. It does not retain a raw credential, infer identity from a request body, select Access/Profile/routes, or authorize proxy traffic. | A Host constructs one immutable typed principal with a closed kind, connection scope, credential revision, and grant-kind allowlist. Authentication returns that principal, never credential material. Explicit revision-increasing rotation temporarily admits old and candidate credentials across publication; revocation closes both. | One authority belongs to one Host connection/generation. Time does not silently change login authority; Host shutdown revokes it. | Construction/alias rejection, local-versus-enrolled scope, grant allowlists, authentication, no-gap commit/abort rotation, cross-connection and skipped-revision rejection, revocation, and race tests. State: local CLI composition is wired; Desktop-App and enrolled-client authenticators remain future consumers. |
| `internal/capturegrant` | Owns control-authorized creation of data-plane capture grants. It does not authenticate HTTP, expose routes, accept caller-selected owner/Access/Profile/account data, or let a remote principal resolve a Server-local cwd. | Consumes one typed `ControlPrincipal`, a typed CaptureRun or ManualCapture request, current ClientOrigin catalog, public Root delivery material, the relevant durable authority, and for runs verified client/workspace evidence. A CaptureRun grant carries a protected MITM authority set plus a distinct managed-credential subset; mere Root eligibility cannot authorize client-auth replacement. Manual owner scope is derived only from the principal. | Issuance is request-scoped. DesktopHost owns the single issuer; durable authorities own grant lifetimes. | Authorization-before-dependency access, local/enrolled ManualCapture owner derivation, opaque context confirmation, local/remote workspace separation, strict request/adapter validation, recognized-client Root decision, immutable authority/subset validation, durable create failures, structural bypass fixtures, and Host integration. State: both issue methods and their local HTTP adapters are composed; all current compiled Access profiles are managed account-backed profiles, so the current issuer derives equal protected and managed sets without claiming the later original-passthrough slice. |
| `internal/capturecontrol` | Owns the narrow HTTP transport for CaptureRun grant creation/lifecycle and ManualCapture context/create/list/detail/rotate/revoke. It does not perform client verification, Root decisions, workspace resolution, durable creation, mutate Access, infer ownership from bodies, reveal an old secret, bind a listener, or publish itself. | The local-CLI adapter authenticates into a typed `ControlPrincipal`; the shared ManualCapture handler also accepts an explicitly authenticated Desktop principal. It strictly decodes, invokes the sole `capturegrant.Issuer`, emits one-time no-store grants, hides internal credential epochs behind opaque ETags, and maps typed failures. Attach/heartbeat/finish consume only the distinct per-run control capability. | Handler calls are request-scoped; DesktopHost owns route registration and listener lifecycle. The runtime-instance control credential is revoked by Host lifecycle; capture capabilities retain their own scopes and lifetimes. | Control/run/manual namespace separation, bounded strict JSON, stale confirmation and ETag rejection, one-time secret projection, observation-stable ETags, grant-to-proxy evidence continuity, invalid routes, stable authority across discovery refresh, real Host registration, and race tests. |
| `internal/manualcaptureclient` | Owns the bounded loopback client for the authenticated ManualCapture contract. It does not discover arbitrary origins, retry mutations, select business routes, persist responses, or log raw grants. | Consumes only a validated private local-discovery session. It validates no-store closed JSON, literal-loopback delivery, Root identity, opaque confirmation/ETag shapes and one-time grants. | One CLI invocation owns and closes the client. A lost mutation response remains ambiguous and is never retried. | Real loopback contract tests cover bearer authentication, create/get/revoke, ETag use, malformed tags and response-shape rejection. |
| `internal/desktopcontrol` | Owns the bounded Desktop control slice. It does not expose SQLite, provider clients, secret values, `SecretRef`, localized strings, absolute workspace paths in route views, or a second Access authority. | Consumes distinct read/write capability grants, an exact Webview origin, readiness/status, Access writer and sole resolver, credential controller, Activity, ConnectionEvent, EgressAttempt, CaptureRun, ManualCapture, workspace-route, approval, and offline boundaries. Produces strict status, active-plan summary/apply, write-only credential, audit pages, shared ManualCapture control, workspace-route list/get/CAS, approval, and offline routes with stable reason/i18n keys. | Request work is bounded by Host-owned HTTP servers. `BeginShutdown` rejects new authenticated operations before listener and ProductRuntime drain. | Exact Host/Origin/Fetch Metadata checks, CORS, scope separation, one-shot bootstrap, strict JSON/body limits, Access/secret/workspace-route CAS and idempotency, active revision load, ManualCapture principal isolation, response redaction, Activity, connection, egress, run/workspace grouping, approval, offline, invalid route, and race tests. This remains a contracted subset, not the full V1 API. |
| `internal/desktopbootstrap` | Owns the one-shot native-shell bootstrap authority. It does not expose the bootstrap nonce in local control discovery or retain App capabilities after exchange. | The daemon emits a bounded descriptor through an inherited pipe. A nonce exchanged outside the Webview returns one session with distinct read/write capabilities, exact generation, loopback base URL, and expiry. | The grant is one-shot, expiring, and explicitly revoked before Host shutdown. | Single-use/replay rejection, wire clone isolation, capability separation, daemon pipe integration, Rust exchange, and shutdown tests. |
| `internal/desktophost` / `internal/desktopdaemon` | Own one Desktop generation, literal proxy/control listeners, complete route composition, discovery/readiness publication, and the packaged Go sidecar adapter. They do not assemble business components in `main`, install Root trust, or publish before both listeners and all routes exist. | DesktopHost consumes typed paths, ProductRuntime options, one exact Webview origin, one immutable revisioned client-release catalog, and bounded TTLs. The daemon consumes an explicit SecretStore factory and inherited bootstrap writer. It produces a ready generation, proxy/control origins, one typed local-CLI principal and capture issuer, local control discovery, and one native-shell descriptor. | Generation lock precedes runtime startup. Discovery publication is the sole readiness commit point. Refresh extends discovery freshness without rotating the generation credential. Shutdown revokes control/App/bootstrap capabilities and readiness, removes discovery, closes listener admission, drains servers and ProductRuntime, then releases the lock; failure is reported as `stop_failed`. | Real loopback listener/control/bootstrap/child-launch integration, exact development versus packaged origin selection, second-generation rejection, stable control authority across discovery refresh, discovery-failure rollback, owner-context shutdown, and race tests. |
| `ui/desktop` | Owns the minimal Tauri native shell and React Desktop UI. It does not assemble runtime services, store capabilities or secrets in Web Storage, read physical SecretStore state directly, treat a launcher user label as authentication, or implement provider traffic. | Rust starts only packaged `vibermated`, consumes its descriptor, exchanges the nonce with a no-proxy loopback client, and passes the session once to `desktop-host.ts`. React uses only the authenticated control client and canonical generated locale catalogs. Activity groups runs by stable machine/workspace, renders pre-Access waiting honestly, changes an Access-scoped route only through typed CAS, creates route-neutral ManualCapture grants through a review-first handoff, and invokes fixed native terminal-command operations without receiving arbitrary filesystem authority. One-time proxy credentials remain component-local until the person dismisses them; list/query state contains only the secret-free capture projection. | The native shell retains the sidecar handle and sends bounded termination on App exit. React polling and mutations share one abort owner and never persist session material. ManualCapture create and rotation are never retried because a lost one-time delivery is ambiguous. | Pinned TypeScript/unit/build checks, active plan loading, credential input clearing, locale switching, strict CaptureRun/workspace-route/ManualCapture wire validation, pre-first-request and multi-Access grouping, route switching/pinned-request copy, one-time ManualCapture handoff and opaque-state revoke, terminal-command native bridge validation, Playwright layout/navigation in both supported locales, frontend structural fixtures, deterministic icon generation, Rust URL/bootstrap/capability/CSP tests, and locked dependency audit. The UI is functional but incomplete. |
| `internal/clientadapter` | Owns explicit fixed-client compound-release verification, catalog revisioning, catalog content pinning, recognition apart from verification, compatibility-feature evidence, typed launch-recipe selection, and the narrow streaming-fallback policy derived from exact evidence. The unpublished catalog remains revision 1 and is pinned in place; after its first externally cited release, a changed catalog must add a revision rather than rewrite an old meaning. Recognition distinguishes a program nobody catalogued from a catalogued client at a version this build has no evidence for. It does not download clients, execute `--version`, trust names/wrappers alone, alter a process, or register releases globally. | Consumes a typed immutable catalog and canonical command evidence. Every declared bounded regular-file artifact must match before it produces verified evidence containing adapter/catalog revisions, install shape, aggregate release hash, recipe, and proven feature bits; only the typed `client_default | core_owned` fallback policy crosses into the launcher wire, while raw feature bits remain internal. A macOS catalog miss may produce typed signer evidence for an exact Developer ID identity, while other misses remain generic and unsafe/ambiguous paths fail. | Pure per-request file verification bounded by context, artifact root, count, and size; no background owner. | No-execution canonical path, wrapper/package/platform/native-child completeness, per-artifact mismatch, symlink escape/path conflict, ambiguous match, input/output alias, exact external fixed-release matches, real Developer ID recognition, team/program substitution rejection, unsigned same-name generic fallback, and Linux no-recognition tests. Catalogued versions alone receive version-specific protocol and launch-policy features; recognized clients require a separate explicit Root-handoff decision and do not inherit those features. |
| `internal/cliinstall` | Owns receipt-backed installation of the packaged terminal command at the fixed per-user path `~/.local/bin/vibermate`. It does not edit a shell profile, choose a source or target from Webview input, overwrite an existing object, or remove an object that no longer matches its receipt. | Consumes the canonical packaged sidecar path, current executable version, user home/config directories, and an injected filesystem. Produces a closed `not_installed/current/source_updated/source_missing/target_missing/conflict` status plus install, refresh, and remove operations. | Each operation is synchronous and process-local. A private receipt binds source, target, target filesystem identity, and version; refresh and removal re-inspect both the receipt and filesystem before mutation. | Temporary-filesystem lifecycle, fixed paths, source canonicalization, collision/refusal, tamper and stale-receipt behavior, CLI JSON adapter tests, fixed Tauri command/capability tests, and native bridge validation. The clean `f26db1195653afedcbdb0071a639ea33690fc34c` development bundle was exercised through install, source-update refresh, status, and `vibermate run`; this is not installer or arbitrary-shell PATH evidence. |
| `internal/runlauncher`, `cmd/vibermate` | Own one-child `vibermate run --` supervision and the local `vibermate capture create` adapter. They do not accept proxy/control origins from arguments, select Access, persist provider secrets, turn `$USER` into identity, or start a daemon. | Both consume private local control discovery. The launcher validates the catalog/adapter/recipe binding, executes the daemon-canonical path, derives workspace scope, reports a bounded display-only `$USER`/`%USERNAME%` label, supervises process I/O/signals and finishes the run. For a fixed Claude/Codex client it matches the effective client origin against the grant's managed subset: a match removes conflicting client auth/base selectors and supplies one non-secret local placeholder, while a non-match preserves the client's own API key/OAuth and origin selection. Exact Claude evidence additionally sets the typed `core_owned` fallback policy, causing the launcher to disable Claude's independent stream-to-complete replay so Core remains the only retry/fallback authority; recognized and generic clients retain `client_default`. Capture create reviews listener/Root/lifetime, confirms through an opaque context token and emits one-time human or shell proxy delivery; routing coordinates are invalid flags. | The CLI owns one invocation. Run cancellation or heartbeat failure terminates the child within a bound. Capture prompts default to no, non-terminal use requires explicit confirmation, network calls have separate bounds around human think time, and mutations are never retried. | Fixture control-server lifecycle, real DesktopHost child launch, managed/default/custom-origin bootstrap, exact-version fallback ownership, unknown-version policy preservation, original-auth preservation, case-insensitive environment isolation, invalid authority subsets, exit/cancellation, environment replacement, inconsistent-grant rejection, capture argument/locale/no-internal-epoch/confirmation/terminal tests, and race tests. A clean packaged v6 report bound to `c16b2f9556fd4123adba9d5a6f72bb7fbb855905` crosses the corrected managed Claude path, and an isolated file-secret dogfood run reaches a completed local provider response. Neither proves original-passthrough or arbitrary-client behavior. |
| `cmd/vibermated` | Is the thin packaged sidecar entrypoint. It does not construct stores, codecs, handlers, listeners, or goroutines in `main`. | Parses only host-resolved absolute directories, one exact known Webview origin, and an inherited bootstrap descriptor; selects the development SecretStore factory and delegates to `desktopdaemon.ProductionOptions` and `Run`. | Signal context owns the daemon; exit waits for bounded Host shutdown. | Argument rejection, daemon integration, and Host lifecycle tests. No release SecretStore selection exists. |
| `cmd/vibermate-acceptance`, `internal/acceptancereport`, `cmd/vibermate-acceptance-verify` | Own the opt-in fixed macOS arm64 packaged-assembly run and its fail-closed evidence verifier. They do not accept secret values, provide a provider fixture, alter Root trust, or run in ordinary deterministic CI. Historical v5 verification is explicit and cannot satisfy a current v6 expectation. | The runner consumes one App bundle, exactly one verified fixed-client path, typed Access route parameters, optional existing development SecretRef, private data/report paths, and a deadline. It derives both sidecars from the App, validates its embedded v2 build manifest, commits one explicit fixed-client connection rule while preserving the default `ask`, and emits a private v6 report with typed client-adapter evidence. The verifier independently consumes trusted source, App, acceptance-executable, and client coordinates; it requires a clean Git top-level checkout at the expected full revision and commit time, rehashes actual artifacts and frozen configuration, and re-parses the actual App manifest. Fixed Codex fallback evidence runs with WebSocket negotiation enabled and requires a Codex-surfaced HTTP 426, the bounded proxy 426-to-HTTP audit, and the exact Runtime Activity reason for the subsequent HTTP Exchange; independent semantic turns use an explicit supported HTTP provider profile. Tool and Hold details derive from typed exercised evidence. | One bounded runner context owns the packaged shell, three direct daemon generations, captured Agent processes, client-private state, and temporary data. Failure paths interrupt/kill owned children and preserve the report; deterministic mode uses a unique missing SecretRef. Verification treats a missing, stale, dirty, malformed, substituted, or unreadable current report or coordinate as failure. | Unit and race tests cover client selection and JSONL trust, actual artifact/configuration substitution, Git checkout freshness, trusted-coordinate swaps, source/toolchain/profile binding, bundle membership, report permissions/redaction, safe Agent evidence, control contracts, daemon framing, explicit connection-policy setup, client-side ConnectionEvent shape, active held-connection discovery, three-source fallback, client-specific tool/streaming detail, and separation of fallback versus semantic invocation profiles. A clean v6 report from source `c16b2f9556fd4123adba9d5a6f72bb7fbb855905` passes 18 of 18 deterministic checks with fixed Claude Code 2.1.220 and is independently verified against the selected source and artifacts. The latest credentialed fixed-Codex report passes 25 of 25 but is bound to historical source `c19cca4eb2842aa00d8e8fc17160b342a111f0b6`; it is not current credentialed evidence. These reports do not prove successful WebSocket semantics, TUI delta rendering, arbitrary client versions, exact JA3/JA4 or HTTP/2 fingerprint parity, or system Root trust. |
| `internal/localdiscovery`, `internal/instanceguard`, `internal/runtimepath`, `internal/loopbackclient` | Own the private local rendezvous primitives: one kernel-backed generation owner, one atomic short-lived local control record, one canonical app-cache layout, and an HTTP client pinned to literal IPv4 loopback. They do not authenticate a principal, publish readiness themselves, carry product configuration, use ambient proxies, or dial external hosts. | Consume explicit absolute paths, runtime identity/PID, discovery expiry, and one generation control credential. Produce a generation-scoped publisher/read boundary and pinned local client. The kernel lock plus instance ID is publication authority; record expiry proves only discovery freshness. | DesktopHost acquires the lock before publication, may republish the same credential with a later expiry, and removes only its own record before releasing ownership. Readers reject stale, malformed, non-private, symlinked, or replaced files. | Contention, lock release, permission/symlink/swap rejection, atomic Host publication/removal ownership, stable-credential freshness refresh, expiry, pinned-dial escape rejection, and race tests. |
| `locales` | Owns the two embedded production message catalogs and deterministic rendering. It does not choose product state or allow one locale to drift independently. | Consumes stable i18n keys and exact named parameters. Produces rendered `en-US` or `zh-CN` text; unsupported environments fall back to `en-US`. Desktop JSON is generated from the same canonical files. | Immutable after constructor validation; no resources. | Nonempty key parity, parameter parity, rendering, detection, generated Desktop drift, UI switching, and repository structural checks. |
| `internal/hostcontract` | Defines valid Desktop and Server runtime descriptors and keeps management authentication distinct from proxy authentication. It does not implement either host shell or transport. | Typed constructors produce closed contracts; ProductRuntime consumes the descriptor and reports its kind. | Value object validated before resources are opened. | Constructor invariants and shared ProductRuntime contract tests. State: `verified contract only`. |
| `internal/repositorycheck` | Owns implementation-language, protocol SDK hot-path isolation, raw external-egress construction, ingress payload-dispatch separation, identity-composition independence, Exchange data-plane Access isolation, production composition uniqueness, canonical locale catalog drift, and existing Desktop frontend boundaries. It does not infer product completion or scan future shapes that do not exist. | Consumes repository files and the two canonical locale catalogs. Official SDK root and subpackage imports are permitted only in oracle tests across protocol core, wire edges, and typed composition; raw network-client construction is limited to typed transports/probes; Exchange cannot import persistence or Access mutation contracts; CaptureRun creation is confined to the typed grant issuer, the proxy cannot import CaptureRun instead of route-neutral capture admission, and DesktopHost must assemble the principal authority, issuer, and HTTP handler in `Start`; the auxiliary and opaque ingress dispatch arms cannot share one case clause; no identity may be built from, or matched against, another identity joined with a delimiter; only `desktop-host.ts` may import Tauri, Web Storage is forbidden, and visible TSX copy must use i18n. | Runs before unit, race, and contract layers. | Each rule has a public-entry injected bad repository fixture and a known-good repository fixture, including the Responses-to-Chat and capture-authority composition roots. State: `wired`. |

## Initialization and Desktop readiness boundaries

`productruntime.Start` currently proves only that:

1. options and the host descriptor were valid;
2. the SQLite database opened with the required driver and connection policy;
3. all embedded migrations completed and schema state resolved from goose;
4. zero or more committed Access aggregates were validated, compiled, and
   projected from SQLite; an empty Access set is valid;
5. the metadata/write-only credential controller was bound to that sole
   projection and the explicit SecretStore;
6. durable Activity, tool-approval, CaptureRun, and ConnectionEvent authorities
   recovered;
7. the owned storage-health goroutine started;
8. strict remote/literal-loopback provider transport, strict original-origin
   transport, and typed resume probes were constructed;
9. the internal Exchange executor was constructed from the sole Access
   resolver, explicit Messages/Responses protocol-path selector, approval
   authority, Activity observer, and gated provider transport;
10. the persistent local Root, immutable shared operation/PathCapability
   catalogs, and
   authenticated handler-only loopback proxy were constructed;
11. the supplied offline-hold coordinator accepted the runtime incarnation.

The resulting ProductRuntime state is `initialized`, not product-ready.
`desktophost.Start` is the only Desktop readiness commit point:

```text
acquire generation lock
  -> start ProductRuntime
  -> bind literal proxy and control listeners
  -> compose bootstrap, App, CaptureRun, and proxy routes
  -> start both bounded HTTP servers
  -> publish private local control discovery
  -> publish Desktop ready
```

Shutdown withdraws readiness and capabilities first, removes discovery, closes
both listener admissions, drains the control server and ProductRuntime tree,
then drains the proxy server and releases generation ownership. ProductRuntime
still never publishes product discovery itself. No Server readiness endpoint or
Server Host exists.

## Access durable and process-local authority

SQLite is the only persistent Access authority. The in-memory projection is a
read-only optimization for the current process and is not transactionally
atomic with SQLite across media. Each write is serialized through:

```text
validate aggregate ownership and references
  -> compile candidate immutable plan and deterministic hash
  -> SQLite compare-and-swap commit
  -> publish immutable active plan
  -> return success
```

The writer lock covers the CAS-to-publication interval, and projection code
rejects a non-advancing revision. Cancellation before commit rolls back and
does not publish. A known commit is published even if the caller cancels
afterward. An ambiguous commit result is reconciled from SQLite using a bounded
store-owned context; an unrecoverable ambiguity returns
`access_commit_outcome_unknown` rather than implying that no write occurred. An
unreconciled ambiguity or a known commit followed by publication failure marks
that Access ID `access_projection_unavailable`. New resolves and writes for the
affected ID then fail closed; already returned immutable snapshot values do not
change. A new process restores a fresh projection from SQLite.

The recovery test creates durable state without publishing it, performs a
normal bounded store shutdown, reopens the SQLite file, and reconstructs the
projection. Responses recovery recompiles the same operation transport,
revision, content, and hash. These tests prove the close/reopen recovery path,
not recovery from `SIGKILL`, a real process crash, operating-system failure, or
power loss.

This slice proves that an old plan handle remains unchanged, a new resolve
observes the new revision, and exact `ClientOrigin` lookup returns immutable
ingress identity evidence. The proxy integration proves authenticated
CONNECT/MITM, path classification, AgentEndpoint revalidation on every request
of a persistent connection, semantic/opaque dispatch, and body-free connection
auditing. ProductRuntime composition proves that its CaptureRun capability,
ingress projection, local Root, proxy handler, and durable journal are the same
runtime-owned objects. The Exchange suite proves one resolver call, exact
client-operation and ingress-evidence revalidation, explicit
Messages/Responses path selection, gated provider invocation, downstream
commit accounting, no retry after Responses-visible semantics, and redacted
Activity recording. Loopback tests prove an exact Responses HTTP request enters
that typed boundary without client auth or hop-by-hop headers, while a matching
WebSocket upgrade receives a bounded 426 before either data plane. They do not
by themselves prove fixed Codex fallback. A separate clean packaged
fixed-client report proves that rejection selects a second authorized HTTP
connection within the same CaptureRun; it does not prove a successful
WebSocket data plane.

The deterministic repository suite additionally proves Host-owned proxy/control listeners, exact
Webview origin selection, bootstrap capability exchange, local control discovery,
readiness publication/withdrawal, one real `vibermate run` child over loopback,
control Access/credential/approval/offline routes, and rollback if discovery
publication fails. Provider behavior in those tests remains fixture-backed.
Credentialed external reply, real fixed-client streaming/tool approval,
packaged `SIGINT`/force-kill recovery, and SQLite reopen are claimed only for
clean artifacts with passing private acceptance reports. The fixed Codex v4
report is superseded by clean v5 reports for fallback, tool, and Hold detail
claims. Neither the
repository tests nor these reports prove Root trust installation, physical offline/sleep,
unmatched-endpoint blind tunneling, or release secret protection.
Writer serialization is process-local; this slice does not prove that two
simultaneous ProductRuntime processes may safely share one database.
