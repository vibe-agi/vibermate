# Runtime Module Map

This file is the implementation index for the current M0 slice. The accepted
design documents and ADRs in the VibeMate design repository remain the
architecture authority. This map records code ownership and current evidence;
it does not redefine product scope.

| Module | Responsibility / non-responsibility | Typed input, output, and authority | Lifecycle | Evidence and current slice |
|---|---|---|---|---|
| `internal/productruntime` | Owns the only production business-component composition sequence, incarnation, internal initialization state, rollback, and shutdown ordering. It composes credential control, Exchange, and the handler-only proxy, but does not bind listeners, publish product readiness/discovery, install system trust, or implement UI. | Consumes `Options`, host contract, runtime paths, one `RuntimeCoordinator`, one SecretStore, approval and Exchange policies, clock, ID source, and security random source. Produces `Runtime`, `RuntimeStatus`, Access writer/resolver and projection health, credential controller, Exchange executor, CaptureRun controller, ConnectionEvent reader, local Root evidence, proxy handler, Activity reader, tool-approval controller, and typed offline enter/resume methods. Provider and original-origin clients remain private implementation dependencies. | Created by `Start`; only fully constructed components enter bounded LIFO rollback/shutdown. Shutdown first closes proxy, CaptureRun, offline, and Exchange admission; revokes active CaptureRuns; closes transports and hijacked proxy operations; drains Exchanges and egress; then stops the local CA, monitor, approvals, durable managers, Access, and SQLite ownership. Cleanup failure leaves `stop_failed`; the tree is not automatically retried. | Lifecycle, complete aggregate recovery/recompilation, exact resume-target composition, real Access-to-Exchange composition for both typed client paths, credential metadata/write composition, runtime CaptureRun/ingress/audit composition, provider-body cancellation, host contracts, and shutdown tests. State: the production compiler, typed protocol-path selector, and path classifier consume the same explicit operation identities for Messages and Responses. Product readiness remains Host-owned. |
| `internal/access` | Owns the typed Access aggregate, separate client/provider network identities, explicit capability catalog, pure compiler, deterministic `PlanHash`, aggregate-local CAS orchestration, immutable active-plan projection, exact `ClientOrigin` lookup, and per-Access projection trust. It does not own SQLite, secret values, protocol wire I/O, provider transport, or control handlers. | Consumes a typed repository, compiler catalog, versioned client-operation definitions, and plan projection. Produces a writer, sole `SnapshotResolver`, typed ingress lookup/catalog boundaries, frozen provider probe identities, and projection health keyed by `AccessID`. Each compiled codec plan owns defensive-copy client operation capabilities and dependency revisions. SQLite aggregates are durable authority; the atomic plan projection is read-only process state. | Restores and recompiles every valid committed aggregate during startup; no rows is valid. Validation and compilation happen before mutation admission. Writer ownership spans CAS through active-plan publication. An indeterminate commit or failed post-commit publication poisons only the affected Access ID. Shutdown rejects new writes, cancels pre-commit work, and drains post-commit publication. | Ownership/reference/operation/capability rejection, deterministic hashing, input/getter alias isolation, stale/concurrent CAS, old/new handles, ClientOrigin conflicts, multi-Access poison isolation, close/reopen recompilation, injected commit ambiguity/publication failure, and race tests. Current compiler accepts one profile/account/route, Direct egress, fixed model, either Anthropic Messages or exact OpenAI Responses as the client operation against OpenAI Chat, and an empty pass-through plugin plan. |
| `internal/accessapply` | Owns the apply-only translation from one complete control-plane DTO to the authoritative Access aggregate. It does not persist, publish, expose a draft model, or define an M0-only singleton API. | Consumes a typed path `AccessID`, expected aggregate revision, and complete value DTO. Produces one `access.WriteCommand`; every owned child receives the same next revision. | Pure construction with no background work or external resources. | Identifier, ownership, origin, provider target, model, and alias-isolation tests. State: the authenticated Desktop apply route consumes this boundary; draft editing remains absent. |
| `internal/protocolcore` | Owns immutable protocol-neutral messages, content blocks, separate item/call identities, tool calls/results, known/unknown usage details, model identities, stop semantics, translation notices, and language-independent failures. It does not parse HTTP, perform I/O, choose an Access, or approve tools. | Constructor-validated values cross codec boundaries; owned byte and collection values are defensively copied, JSON objects reject duplicate names, and failed/cancelled outcomes cannot masquerade as successful response stop reasons. | Pure values with no background work or external resources. | Deep-copy, known-zero versus unknown usage, item/call separation, duplicate correlation rejection, response/tool consistency, provider-extension isolation, fuzz, and race tests. |
| `internal/protocolpath` | Owns the typed composition boundary between one client protocol edge and one provider protocol edge around neutral IR. It does not resolve plans, perform transport I/O, retrieve credentials, or provide a global codec registry. | Explicit `ClientCodec` and `BackendCodec` interfaces decode and encode immutable protocol-core values and return explicit translation reports. A client stream encoder is injected into the existing backend stream decoder. Each typed path binds exact client-operation IDs and validates the frozen Access codec ID, revision, operations, and both dialects; an explicitly assembled immutable selector chooses the one matching path. Provider requests own cloned method/path/header/body state. | Pure composition with no background work. | Constructor/duplicate rejection, operation/dialect/revision mismatch, request alias isolation, response-report propagation, and bilateral codec tests. The streaming bridge remains an explicit pair-specific seam. |
| `internal/ssewire` | Owns bounded incremental Server-Sent Event framing. It does not interpret protocol JSON, infer success from EOF, reconnect sockets, or perform network I/O. | A caller-owned decoder accepts arbitrary byte fragments and emits only complete blank-line-terminated events under explicit byte/event limits; the encoder emits canonical frames. | Caller-owned state only; no goroutines or external resources. | LF/CRLF, multiline fields, byte fragmentation, limits, partial EOF, round-trip, fuzz, and race tests. |
| `internal/anthropicchat` | Implements the bounded Anthropic Messages client edge and the reusable OpenAI Chat backend edge, including the sole Chat response/SSE decoder and complete-tool barrier. It does not select plans, retrieve credentials, send HTTP, or localize errors. | The Anthropic client edge converts wire values to and from protocol-core IR. The Chat backend builds frozen requests, uses an explicit per-request tool catalog, and drives an injected typed client stream encoder only after validated semantic transitions. Official SDKs remain test-only oracles. | Pure request/response and incremental stream state owned by the caller. | SDK-oracle fixtures, per-byte streaming, model separation, complete/incomplete/parallel and custom/namespaced tools, unknown provider-tool rejection, no-leak decisions, unsupported-shape failures, fuzz, and race tests. No network execution is claimed. |
| `internal/openairesponses` | Owns the typed OpenAI Responses client wire edge. It does not send provider traffic, choose Access, retrieve credentials, decode provider Chat traffic, or import the official SDK in production. | Bounded strict JSON is decoded into immutable protocol-core messages with distinct item/call identities, function/custom/namespace tools, reasoning intent, and explicit translation notices. Complete JSON and incremental named SSE events are encoded with deterministic client-safe response/item/call identities, strict ordering, bounded state, and explicit unknown usage. Stored/background execution is rejected explicitly. | Pure caller-owned codec and stream-encoder state; no goroutines or external resources. | Fixed Codex request, complete response, text/function/custom SSE, function output, usage, failure/cancellation oracle fixtures; malformed ordering, duplicate terminal, unknown extension, incomplete correlation, alias, fuzz, and race tests. State: the exact HTTP create operation is selected by production Exchange/ingress; fixed-client packaged evidence remains pending. |
| `internal/responseschat` | Explicitly composes the Responses client edge with the existing Chat backend edge. It does not own a registry, transport, Access selection, credentials, approval, or ingress. | A typed constructor produces one versioned, operation-bound `protocolpath.Path`; Chat custom/namespaced tools are reversibly represented through a per-request catalog, while item identity and unsupported controls produce stable translation notices. | Pure composition with caller-owned stream state and no background work. | Fixed request-to-Chat body, history correlation, deterministic complete response, incremental text, approval-fenced custom tool, unknown provider-tool, and production selector tests. State: its identity, revision, dialects, and exact create operation compile into the sole Access plan and are selected explicitly by production Exchange. |
| `internal/exchange` | Owns one frozen-plan execution pipeline, action admission, ingress-binding and client-operation revalidation, provider attempt accounting, retry safety, downstream publication, stream idle budgets, translation evidence, and the tool-decision barrier. It does not own a listener, resolve SQLite directly, mutate Access, retrieve a secret directly, or expose a provider bypass. | Consumes `ClientRequest`, immutable ingress and exact operation evidence, the sole `SnapshotResolver`, one explicitly assembled typed protocol-path selector, mandatory action admission, gated provider, tool-decision gate, attempt observer, and bounded policies. It resolves exactly once, passes only frozen plan values downstream, and returns merged immutable translation notices without semantic payloads. | Each Exchange has an owner-derived context and an action lease. `BeginShutdown` rejects and cancels operations; transport closure can release blocked bodies before bounded `Drain`. | Messages and Responses complete/streaming output, report propagation, one-resolve proof, operation and stale-endpoint rejection, provider-only plan revision compatibility, retry ledger after Responses-visible output, semantic idle, malformed streams, keepalive cancellation, both client tool barriers, planned hold/resume, shutdown, concurrent multi-Access isolation, and race tests. State: executable for exact frozen Messages and Responses HTTP operations through the same gated provider boundary. |
| `internal/capturerun` | Owns short-lived child attribution and separate proxy/control bearer capabilities. It never persists raw capabilities, child arguments, credentials, or client semantic payloads and does not launch processes or expose HTTP. | Consumes typed create/attach/heartbeat/finish commands, the consulted client-catalog revision, optional verified adapter evidence, and a SQLite repository. Produces one-time `LaunchGrant`, redacted views, and frozen proxy authorization evidence. SQLite stores domain-separated capability hashes plus non-secret run/catalog/compound-release evidence. | Startup expires stale records. Admission and active operations are bounded; shutdown rejects new work, drains current calls, and revokes every active run. | Capability separation/rejection, generic and verified adapter binding, expiry/recovery, schema-11 upgrade, attach conflicts, heartbeat, shutdown revocation, close/reopen, alias isolation, and race tests. State: composed into ProductRuntime and exposed only through launcher/run-scoped DesktopHost routes. |
| `internal/connectionevent` | Owns the durable body-free connection phase timeline. It does not store URL paths, headers, bodies, raw capabilities, TLS bytes, or semantic Exchange content. | Consumes typed attempted/decided/connected/terminal evidence and a SQLite repository. Produces immutable paginated records and bounded per-connection timelines. | Startup appends failed terminal records for interrupted connections. New operations are rejected during shutdown and active journal writes drain within the caller context. | Phase-transition, redaction, pagination, interrupted recovery, alias isolation, shutdown, repository reopen, and race tests. State: loopback proxy journal is wired. |
| `internal/operationcatalog` | Owns the explicit, versioned built-in client API operation definitions. It does not select an Access, instantiate codecs, perform dispatch, or provide a global implementation registry. | A typed constructor produces immutable definitions covering dialect, method, exact/prefix path, HTTP/WebSocket transport, semantic category, body/replay limits, codec feature, query policy, and egress bearing. Access compilation and path classification consume the same definitions. | Pure immutable values; no resources or goroutines. | Exact Responses and Messages HTTP operations, explicit unsupported Responses WebSocket upgrade, control-plane opaque classification, upload/background-adjacent/Realtime/foreign-semantic rejection, ID uniqueness, and alias tests. |
| `internal/pathcapability` | Compiles the operation catalog into canonical method/path/query dispatch. It does not define model API paths, parse request bodies, choose Access plans, perform I/O, or register codec implementations globally. | Consumes immutable `access.ClientOperationDefinition` values and classifies a frozen client dialect and canonical request target into semantic, auxiliary, opaque, or unsupported capability plus operation identity, revision, transport, replay/body limits, and egress policy. Exact definitions sharing a path remain distinct by canonical method. | Pure immutable catalog; no resources or goroutines. | Messages semantic/auxiliary, exact Responses HTTP semantic and unsupported WebSocket classifications, opaque control/unknown fallback, explicit management/upload/media/batch/Realtime/foreign-semantic rejection, malformed target, method/query mismatch, alias, and race tests. State: consumed by the production proxy and Exchange operation-evidence boundary. |
| `internal/localca` | Owns one installation-persistent local Root key/certificate and exact-host leaf issuance. It does not install trust, alter OS settings, accept arbitrary certificate identities, or own a listener. | Consumes an explicit private directory, clock, and security random source. Produces immutable public Root evidence and server certificates for validated exact DNS/IP hosts. Filesystem state is the Root authority. | Open validates or creates private Root artifacts atomically. Leaf cache is process-local; shutdown closes issuance and destroys owned private-key references. | Permissions, symlink and malformed-state rejection, reopen continuity, exact SANs, returned-value isolation, concurrent issuance, and shutdown tests. State: ProductRuntime owns the authority; trust installation is deferred. |
| `internal/loopbackproxy` | Owns authenticated CONNECT handling, ClientHello observation, exact AgentEndpoint matching, MITM request dispatch, persistent-connection endpoint revalidation, semantic Exchange entry, auxiliary/opaque original-origin forwarding, explicit unsupported-upgrade rejection, and ConnectionEvent updates. It does not bind a listener, publish discovery, install a Root, retrieve provider secrets, or implement unmatched-endpoint blind tunneling. | Consumes CaptureRun proxy authorization with frozen catalog/adapter evidence, `IngressResolver`, immutable path catalog, Exchange executor, gated original client, local CA, ConnectionEvent journal, and Exchange ID source. Produces an `http.Handler` plus explicit admission/drain lifecycle. Semantic requests carry only owned body, ingress, ClientHello, and typed operation evidence across the Exchange boundary; client auth and hop-by-hop headers do not. | Runtime owner cancellation closes admission and hijacked connections. `BeginShutdown` rejects new CONNECT work; `Drain` waits for bounded active operations. | Auth-before-cert/data-plane, exact origin/SNI, real CONNECT/MITM, exact Messages/Responses dispatch, adapter-feature-gated bounded Responses WebSocket 426, generic-client 422 rejection, stale endpoint rejection on reused connections for both dialects, audit persistence, hijacked-connection shutdown, and race tests. State: DesktopHost owns its literal loopback listener and publishes it only after route completion; repository tests still do not prove that real fixed Codex performs the HTTP fallback. |
| `internal/activity` | Owns the durable redacted operational timeline. It does not store prompts, completions, credentials, headers, raw tool arguments, or provider error bodies. | Consumes validated stable event kinds, identifiers, language-independent reason codes, and optional redacted transport evidence. Produces immutable paginated records. | Operations use the shared SQLite admission gate through the repository; the manager rejects new work and drains active records during shutdown. | Pagination, close/reopen persistence, input/output alias isolation, transport-evidence validation, shutdown, and race tests. Exchange attempts are wired through ProductRuntime. |
| `internal/toolapproval` | Owns durable fail-closed approval of complete tool-intent groups. It does not expose raw arguments, execute tools, or grant persistent policy. | Consumes safe tool identities plus frozen Access revision and PlanHash. Produces pending views with stable i18n keys and an allow-once or deny decision for the complete group. SQLite is the decision authority. | Startup cancels orphaned pending approvals. Each caller waits with a deadline; decisions are CAS- and idempotency-keyed. Shutdown cancels pending rows, releases waiters, rejects admission, and drains active work. | Complete-group blocking/release, denial, idempotent replay, stale decision rejection, timeout/cancellation, recovery, redaction, authenticated control decisions, UI binding, and race tests. Real Claude behavior is established only by a passing frozen-artifact M0 report. |
| `internal/runtimepersistence` | Owns SQLite connection policy, embedded forward migrations, operation admission/cancellation/drain, runtime metadata, complete Access aggregate rows, transactional CAS, Activity, tool approval, CaptureRun, and ConnectionEvent rows, plus schema-state reads. It does not compile or publish active plans, retain raw capabilities, retrieve secrets, send provider traffic, provide backups, or expose UI queries. | Consumes an absolute database path and bounded connection/commit-reconciliation policy. Produces a file-backed store, `SchemaStateReader`, and typed Access, Activity, approval, CaptureRun, and ConnectionEvent repositories. The highest applied goose migration version is the only schema revision authority; each Access aggregate independently owns its business revision. All Access child rows are written in the same transaction as the Access root CAS. | Opened first; migrations 1–12 complete before initialization. CaptureRun rows bind the consulted client catalog revision and any verified compound-release evidence to the proxy capability. All repository work uses one operation gate. Ambiguous Access commits are reread using a bounded store-owned context. Shutdown closes admission, cancels and drains owned operations, then closes SQLite. | File-backed reopen, pragmas, permissions, held-transaction deadline, complete aggregate CAS/rollback, ClientOrigin uniqueness, commit reconciliation, Activity including credential replacement, approval/CaptureRun/ConnectionEvent persistence and recovery, corruption, cancellation, and schema continuity tests. State: `wired` and component-verified. |
| `internal/offlinehold` | Owns the mandatory external-egress admission seam and planned enter/drain/probe/FIFO-release state machine. It does not own sockets, secrets, OS reachability, persistence, reactive cursor resume, or UI. | A logical action and Enter share one mutex cut: actions admitted before Enter may finish, while later actions queue at egress. Every acquire carries that action plus a complete frozen non-secret network identity; provider identities bind Access revision and PlanHash. Queues are count/byte/time bounded. | One runtime incarnation binds the gate. Enter waits for pre-cut actions and active egress before `SafeToDisconnect`; Resume probes exact queued identities and releases only successful targets. Shutdown rejects queued work and drains actions and leases. | Deterministic pre/post-Enter ordering, plan-change target isolation, Safe-to-disconnect fencing, failed-probe zero release, bounded FIFO/concurrency, cancellation, revision conflicts, alias isolation, shutdown, and race tests. Physical network change remains unverified. |
| `internal/secretstore` / `internal/hostsecret` | Define canonical `SecretRef`, destroyable bounded values, redacted metadata, the typed host Factory/Store boundary, and a cross-platform development file driver. They do not put secrets in SQLite, log/list values, return values to UI, or select a backend inside ProductRuntime. | ProductRuntime receives exactly one explicit Store. Ordinary builds select a private file factory with `!vibermate_native_secrets`; its CAS document is plaintext-equivalent at rest. Native release selection is not present in this stage. | Values are caller-owned and explicitly destroyed. The development Store serializes CAS and replaces a private file atomically for the host platform; reopen reconstructs secret revisions. | Reference/value destruction, metadata/CAS conflict, permissions, symlink/path rejection, replacement failure, concurrent access, close/reopen, and race tests. Development storage is not release protection. |
| `internal/accesscredential` | Binds credential metadata and write-only replacement to account bindings in the active Access plan. It does not return `SecretRef` or secret bytes, inspect SQLite, select a backend, or read a secret merely to render control state. | Consumes the sole `SnapshotResolver` and one explicit SecretStore. Produces redacted state/revision views and CAS replacement results scoped by `AccessID`, profile ID, and account binding ID. | Request-scoped; ProductRuntime owns the underlying resolver and SecretStore lifetimes. | Active-plan ownership, cross-profile rejection, metadata-only inspection, unavailable mapping, replacement CAS, response redaction, and race tests. |
| `internal/transportprofile` | Owns frozen observed/standard TLS profile material, strict connector construction, explicit fallback decisions, and redacted transport evidence. It does not select Access plans, retrieve credentials, or claim exact browser/Agent fingerprint fidelity. | Consumes the compiled Access transport fingerprint plan plus an observed ClientHello where requested. Produces a strict verified TLS connection and immutable requested/effective/fallback/ALPN/HTTP evidence. | Each connector operation is context-bounded; no global pool, registry, or background owner exists. | ClientHello capture bounds, extension sanitization, SNI/ALPN replacement, TLS-version enforcement, fallback classification, strict certificate rejection, and race tests. Current transport is HTTP/1.1 and does not prove exact JA3/JA4 or HTTP/2 fingerprint parity. |
| `internal/providertransport` | Owns the final M0 provider request, AuthDriver application, typed strict-TLS versus literal-loopback-cleartext dispatch, response-idle ownership, typed resume probing, and shutdown. It does not resolve Access plans, translate protocol semantics, expose a raw client, or permit non-loopback cleartext. | Consumes a frozen compiled target/action, the mandatory coordinator, one typed authenticator, SecretStore reader, and transport profile. Egress lease acquisition precedes secret read and any outbound byte. Origin, HTTP authority, network host, and optional SNI remain separate frozen fields. Remote targets use strict system-root TLS and fingerprint selection. The Direct-only loopback exception disables ambient proxies and verifies the exact TCP peer before authenticated HTTP write. | Operations are admitted into the client lifecycle, hold action/egress leases through response EOF or close, and are canceled and drained during shutdown. | Lease-before-secret/transport ordering, target/path/Host/SNI, remote/localhost/LAN HTTP rejection, literal-loopback peer validation, no-write on peer mismatch, redirect denial, header stripping, value destruction, strict TLS, transport-appropriate no-credential probes, idle timeout, cancellation, shutdown, and race tests. The ProductRuntime Exchange invokes this typed boundary. |
| `internal/originaltransport` | Owns strict TLS forwarding and probing for approved non-model original-origin operations. It does not select an Agent endpoint, classify paths, carry provider credentials, or permit ambient proxy behavior. | Consumes a fully frozen request, action lease, exact origin/authority/SNI, and mandatory coordinator. It returns a response whose body owns the egress lease. | Client admission, cancellation, body closure, and bounded shutdown mirror the provider transport. | Lease ordering, TLS verification, redirect denial, header/body ownership, exact probe target, cancellation, shutdown, and race tests. The loopback proxy invokes it only after CaptureRun, endpoint, and path authorization. |
| `internal/capturecontrol` | Owns the narrow launcher-to-CaptureRun HTTP contract and its two capability scopes. It does not mutate Access, reveal secrets, accept browser origins, bind a listener, or publish itself. | Consumes a launcher authority, CaptureRun controller, verified client adapter, active ClientOrigin catalog, proxy origin, and public Root evidence. Produces bounded create/attach/heartbeat/finish routes and one redacted launch grant whose catalog revision, adapter evidence, launch recipe, canonical executable, and proxy capability are mutually bound. | Handler calls are request-scoped; DesktopHost owns route registration and listener lifecycle. Launcher capabilities expire with discovery; run capabilities remain scoped to one run. | Launcher/run capability separation, bounded strict JSON, complete compound executable verification, grant-to-proxy evidence continuity, immutable authority lists, invalid route, expiry, real Host registration, and handler race tests. |
| `internal/desktopcontrol` | Owns the bounded M0 Desktop control slice. It does not expose SQLite, provider clients, secret values, `SecretRef`, localized strings, or a second Access authority. | Consumes distinct read/write capability grants, an exact Webview origin, readiness/status, Access writer and sole resolver, credential controller, Activity, ConnectionEvent, approval, and offline boundaries. Produces strict status, active-plan summary/apply, write-only credential, Activity, connection, approval, and offline routes with stable reason/i18n keys. | Request work is bounded by Host-owned HTTP servers. `BeginShutdown` rejects new authenticated operations before listener and ProductRuntime drain. | Exact Host/Origin/Fetch Metadata checks, CORS, scope separation, one-shot bootstrap, strict JSON/body limits, Access/secret CAS and idempotency, active revision load, response redaction, Activity, connection, approval, offline, invalid route, and race tests. This is a contracted M0 subset, not the full V1 API. |
| `internal/desktopbootstrap` | Owns the one-shot native-shell bootstrap authority. It does not expose the bootstrap nonce in launcher discovery or retain App capabilities after exchange. | The daemon emits a bounded descriptor through an inherited pipe. A nonce exchanged outside the Webview returns one session with distinct read/write capabilities, exact generation, loopback base URL, and expiry. | The grant is one-shot, expiring, and explicitly revoked before Host shutdown. | Single-use/replay rejection, wire clone isolation, capability separation, daemon pipe integration, Rust exchange, and shutdown tests. |
| `internal/desktophost` / `internal/desktopdaemon` | Own one Desktop generation, literal proxy/control listeners, complete route composition, discovery/readiness publication, and the packaged Go sidecar adapter. They do not assemble business components in `main`, install Root trust, or publish before both listeners and all routes exist. | DesktopHost consumes typed paths, ProductRuntime options, one exact Webview origin, one immutable revisioned client-release catalog, and bounded TTLs. The daemon consumes an explicit SecretStore factory and inherited bootstrap writer. It produces a ready generation, proxy/control origins, launcher discovery, and one native-shell descriptor. | Generation lock precedes runtime startup. Discovery publication is the sole readiness commit point. Shutdown revokes capabilities/readiness, removes discovery, closes listener admission, drains servers and ProductRuntime, then releases the lock; failure is reported as `stop_failed`. | Real loopback listener/control/bootstrap/child-launch integration, exact development versus packaged origin selection, second-generation rejection, launcher rotation, discovery-failure rollback, owner-context shutdown, and race tests. |
| `ui/desktop` | Owns the minimal Tauri native shell and React Desktop UI. It does not assemble runtime services, store capabilities or secrets in Web Storage, read physical SecretStore state directly, or implement provider traffic. | Rust starts only packaged `vibermated`, consumes its descriptor, exchanges the nonce with a no-proxy loopback client, and passes the session once to `desktop-host.ts`. React uses only the authenticated control client and canonical generated locale catalogs. | The native shell retains the sidecar handle and sends bounded termination on App exit. React polling and mutations share one abort owner and never persist session material. | Pinned TypeScript/unit/build checks, active revision loading, credential input clearing, locale switching, frontend structural fixtures, deterministic icon generation, Rust URL/bootstrap/capability/CSP tests, and locked dependency audit. The UI is functional but incomplete. |
| `internal/clientadapter` | Owns explicit fixed-client compound-release verification, catalog revisioning, compatibility-feature evidence, and typed launch-recipe selection. It does not download clients, execute `--version`, trust names/wrappers alone, alter a process, or register releases globally. | Consumes a typed immutable catalog and canonical command evidence. Every declared bounded regular-file artifact must match before it produces verified evidence containing adapter/catalog revisions, install shape, aggregate release hash, recipe, and proven feature bits; misses produce generic evidence and unsafe/ambiguous paths fail. | Pure per-request file verification bounded by context, artifact root, count, and size; no background owner. | No-execution canonical path, wrapper/package/platform/native-child completeness, per-artifact mismatch, symlink escape/path conflict, ambiguous match, input/output alias, exact external fixed 0.145.0 match, and external 0.146.0 generic fallback tests. The catalog currently grants Codex-specific behavior only to the fixed global npm 0.145.0 Darwin arm64 shape; other versions and layouts remain usable generic clients. |
| `internal/runlauncher`, `cmd/vibermate` | Own one-child `vibermate run --` supervision. They do not accept proxy/control origins from arguments, select Access, persist provider secrets, or start a daemon. | Consume private launcher discovery and localized CLI keys. The launcher validates the catalog/adapter/recipe binding, executes the daemon-canonical path, builds an owned child environment, attaches the PID, heartbeats, forwards process I/O/signals, and finishes the run. Fixed Codex receives only the authenticated proxy, the same local Root through `SSL_CERT_FILE`, and a stable non-secret client placeholder after conflicting proxy/CA/base-URL/credential variables are removed. | The CLI remains the child supervisor. Context cancellation or heartbeat failure terminates the child within a bound; all control calls are timed and the control client closes idle connections. | Fixture control-server lifecycle, real DesktopHost child launch, exit propagation, heartbeat failure, cancellation, fixed-Codex SIGINT/finish convergence, environment replacement, protected `NO_PROXY` stripping, inconsistent-grant rejection, localization, and race tests. These repository tests do not yet prove packaged real-Codex network behavior. |
| `cmd/vibermated` | Is the thin packaged sidecar entrypoint. It does not construct stores, codecs, handlers, listeners, or goroutines in `main`. | Parses only host-resolved absolute directories, one exact known Webview origin, and an inherited bootstrap descriptor; selects the development SecretStore factory and delegates to `desktopdaemon.ProductionOptions` and `Run`. | Signal context owns the daemon; exit waits for bounded Host shutdown. | Argument rejection, daemon integration, and Host lifecycle tests. No release SecretStore selection exists. |
| `cmd/vibermate-acceptance` | Owns the opt-in fixed macOS arm64 packaged-assembly gate. It does not accept secret values, provide a provider fixture, alter Root trust, or run in ordinary deterministic CI. | Consumes one App bundle, exact fixed-Claude path, typed Access route parameters, optional existing development SecretRef, private data/report paths, and a deadline. It derives both sidecars from the App, validates its embedded build manifest, and emits a private v3 report. | One bounded runner context owns the packaged shell, three direct daemon generations, captured Agent processes, and temporary data. Failure paths interrupt/kill owned children and preserve the report; deterministic mode uses a unique missing SecretRef. | Unit tests cover artifact/source/toolchain/profile binding, bundle membership, report permissions/redaction, safe Agent evidence, control contracts, daemon framing, and deterministic hold/audit helpers. A real report separately proves the selected frozen artifact; runner code alone does not. |
| `internal/launcherdiscovery`, `internal/instanceguard`, `internal/runtimepath`, `internal/loopbackclient` | Own the private local rendezvous primitives: one kernel-backed generation owner, one atomic short-lived launcher record, one canonical app-cache layout, and an HTTP client pinned to literal IPv4 loopback. They do not publish readiness themselves, carry product configuration, use ambient proxies, or dial external hosts. | Consume explicit absolute paths, runtime identity/PID, expiry, and one launcher capability. Produce a generation-scoped publisher/read boundary and pinned local client. The kernel lock plus instance ID is publication authority. | DesktopHost acquires the lock before publication and removes only its own record before releasing ownership. Readers reject stale, malformed, non-private, symlinked, or replaced files. | Contention, lock release, permission/symlink/swap rejection, atomic Host publication/removal ownership, rotation, expiry, pinned-dial escape rejection, and race tests. |
| `locales` | Owns the two embedded production message catalogs and deterministic rendering. It does not choose product state or allow one locale to drift independently. | Consumes stable i18n keys and exact named parameters. Produces rendered `en-US` or `zh-CN` text; unsupported environments fall back to `en-US`. Desktop JSON is generated from the same canonical files. | Immutable after constructor validation; no resources. | Nonempty key parity, parameter parity, rendering, detection, generated Desktop drift, UI switching, and repository structural checks. |
| `internal/hostcontract` | Defines valid Desktop and Server runtime descriptors and keeps management authentication distinct from proxy authentication. It does not implement either host shell or transport. | Typed constructors produce closed contracts; ProductRuntime consumes the descriptor and reports its kind. | Value object validated before resources are opened. | Constructor invariants and shared ProductRuntime contract tests. State: `verified contract only`. |
| `internal/repositorycheck` | Owns implementation-language, protocol SDK hot-path isolation, raw external-egress construction, Exchange data-plane Access isolation, canonical locale catalog drift, and existing Desktop frontend boundaries. It does not infer product completion or scan future shapes that do not exist. | Consumes repository files and the two canonical locale catalogs. Official SDK root and subpackage imports are permitted only in oracle tests across protocol core, wire edges, and typed composition; raw network-client construction is limited to typed transports/probes; Exchange cannot import persistence or Access mutation contracts; only `desktop-host.ts` may import Tauri, Web Storage is forbidden, and visible TSX copy must use i18n. | Runs before unit, race, and contract layers. | Each rule has a public-entry injected bad repository fixture and a known-good repository fixture, including the Responses-to-Chat composition root. State: `wired`. |

## Initialization and Desktop readiness boundaries

`productruntime.Start` currently proves only that:

1. options and the host descriptor were valid;
2. the SQLite database opened with the required driver and connection policy;
3. all embedded migrations completed and schema state resolved from goose;
4. zero or more committed Access aggregates were validated, compiled, and
   projected from SQLite; an empty Access set is valid;
5. the metadata/write-only credential controller was bound to that sole
   projection and the explicit SecretStore;
6. durable Activity, tool-approval, CaptureRun, and ConnectionEvent authorities
   recovered;
7. the owned storage-health goroutine started;
8. strict remote/literal-loopback provider transport, strict original-origin
   transport, and typed resume probes were constructed;
9. the internal Exchange executor was constructed from the sole Access
   resolver, explicit Messages/Responses protocol-path selector, approval
   authority, Activity observer, and gated provider transport;
10. the persistent local Root, immutable shared operation/PathCapability
   catalogs, and
   authenticated handler-only loopback proxy were constructed;
11. the supplied offline-hold coordinator accepted the runtime incarnation.

The resulting ProductRuntime state is `initialized`, not product-ready.
`desktophost.Start` is the only Desktop readiness commit point:

```text
acquire generation lock
  -> start ProductRuntime
  -> bind literal proxy and control listeners
  -> compose bootstrap, App, CaptureRun, and proxy routes
  -> start both bounded HTTP servers
  -> publish private launcher discovery
  -> publish Desktop ready
```

Shutdown withdraws readiness and capabilities first, removes discovery, closes
both listener admissions, drains the control server and ProductRuntime tree,
then drains the proxy server and releases generation ownership. ProductRuntime
still never publishes product discovery itself. No Server readiness endpoint or
Server Host exists.

## Access durable and process-local authority

SQLite is the only persistent Access authority. The in-memory projection is a
read-only optimization for the current process and is not transactionally
atomic with SQLite across media. Each write is serialized through:

```text
validate aggregate ownership and references
  -> compile candidate immutable plan and deterministic hash
  -> SQLite compare-and-swap commit
  -> publish immutable active plan
  -> return success
```

The writer lock covers the CAS-to-publication interval, and projection code
rejects a non-advancing revision. Cancellation before commit rolls back and
does not publish. A known commit is published even if the caller cancels
afterward. An ambiguous commit result is reconciled from SQLite using a bounded
store-owned context; an unrecoverable ambiguity returns
`access_commit_outcome_unknown` rather than implying that no write occurred. An
unreconciled ambiguity or a known commit followed by publication failure marks
that Access ID `access_projection_unavailable`. New resolves and writes for the
affected ID then fail closed; already returned immutable snapshot values do not
change. A new process restores a fresh projection from SQLite.

The recovery test creates durable state without publishing it, performs a
normal bounded store shutdown, reopens the SQLite file, and reconstructs the
projection. Responses recovery recompiles the same operation transport,
revision, content, and hash. These tests prove the close/reopen recovery path,
not recovery from `SIGKILL`, a real process crash, operating-system failure, or
power loss.

This slice proves that an old plan handle remains unchanged, a new resolve
observes the new revision, and exact `ClientOrigin` lookup returns immutable
ingress identity evidence. The proxy integration proves authenticated
CONNECT/MITM, path classification, AgentEndpoint revalidation on every request
of a persistent connection, semantic/opaque dispatch, and body-free connection
auditing. ProductRuntime composition proves that its CaptureRun capability,
ingress projection, local Root, proxy handler, and durable journal are the same
runtime-owned objects. The Exchange suite proves one resolver call, exact
client-operation and ingress-evidence revalidation, explicit
Messages/Responses path selection, gated provider invocation, downstream
commit accounting, no retry after Responses-visible semantics, and redacted
Activity recording. Loopback tests prove an exact Responses HTTP request enters
that typed boundary without client auth or hop-by-hop headers, while a matching
WebSocket upgrade receives a bounded 426 before either data plane. They do not
yet prove fixed Codex fallback.

The deterministic repository suite additionally proves Host-owned proxy/control listeners, exact
Webview origin selection, bootstrap capability exchange, launcher discovery,
readiness publication/withdrawal, one real `vibermate run` child over loopback,
control Access/credential/approval/offline routes, and rollback if discovery
publication fails. Provider behavior in those tests remains fixture-backed.
Credentialed external reply, real Claude streaming/tool approval, packaged
`SIGINT`/force-kill recovery, and SQLite reopen are claimed only for a clean
artifact with a passing private M0 v3 report. Neither the repository tests nor
that report prove Root trust installation, physical offline/sleep,
unmatched-endpoint blind tunneling, or release secret protection.
Writer serialization is process-local; this slice does not prove that two
simultaneous ProductRuntime processes may safely share one database.
